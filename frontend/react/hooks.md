# React Hooks

## Purpose

Guide reusable stateful behavior in React.

## Rules

- Use hooks for focused, reusable stateful behavior.
- Keep hooks testable.
- Do not hide unrelated side effects inside generic hooks.
- Include dependencies explicitly.
- Return values with stable, understandable names.
- Avoid hooks that become broad service layers.
