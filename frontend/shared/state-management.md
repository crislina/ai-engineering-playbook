# State Management

## Purpose

Keep frontend state understandable, local, and aligned with user workflows.

## Principles

- Keep state as local as possible.
- Model loading, empty, error, and success states explicitly.
- Keep derived data derived instead of duplicating it in state.
- Use server-state tools for remote data when available.
- Avoid duplicating server data into local state unless needed for editing workflows.
